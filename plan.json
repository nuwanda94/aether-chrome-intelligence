{
  "title": "Aether v2.2 — hardening from principal review",
  "repo": "nuwanda94/aether-chrome-intelligence",
  "cadence": "one pending item per hourly run",
  "phases": [
    {"id": "F", "name": "Foundation", "summary": "MV3 shell, side panel, content script, service worker, offscreen, Nano + heuristic engines."},
    {"id": "1", "name": "Side panel & state", "summary": "Editable fields, confidence, undo, conflict modal, source verify."},
    {"id": "2", "name": "Language & Unicode", "summary": "Language-parameterized extraction, JP/DE/EN names and addresses."},
    {"id": "3", "name": "Self-healing harness", "summary": "Link scoring, background tabs, merge, circuit breakers."},
    {"id": "4", "name": "Performance & cache", "summary": "DOM sanitize, chunking, chrome.storage schema cache."},
    {"id": "5", "name": "Security & governance", "summary": "CSP, PII mask, no page JS in extension context."},
    {"id": "Q", "name": "Quality & ship", "summary": "Tests, options page, i18n, store listing."},
    {"id": "H", "name": "Hardening (v2.2)", "summary": "Provenance, schema validation, heuristic breadth, permission surface, gold-set metrics, CI."}
  ],
  "items": [
    {"id": "F01", "phase": "F", "status": "done", "title": "Manifest V3 scaffold", "detail": "sidePanel, service worker modules, content script, offscreen document, icons, CSP.", "files": ["manifest.json", "background/service-worker.js"], "done_when": "Unpacked load opens the side panel from the action icon."},
    {"id": "F02", "phase": "F", "status": "done", "title": "Sanitize + Markdown serialize", "detail": "Strip scripts/SVG/trackers; convert semantic DOM to Markdown; collect links.", "files": ["content/content-script.js", "lib/engine.js"], "done_when": "AETHER_SERIALIZE returns url, title, lang, markdown, links."},
    {"id": "F03", "phase": "F", "status": "done", "title": "Nano + heuristic inference", "detail": "Prompt API when available; schema JSON; heuristic fallback.", "files": ["lib/nano.js", "lib/engine.js"], "done_when": "inferDocument always returns a CompanyRecord."},
    {"id": "1A", "phase": "1", "status": "done", "title": "Editable dossier UI", "detail": "Inline fields, C-suite cards, confidence badges, JSON/MD export, PII toggle.", "files": ["sidepanel/index.html", "sidepanel/panel.js", "sidepanel/panel.css"], "done_when": "Every field is editable; export downloads a file."},
    {"id": "3A", "phase": "3", "status": "done", "title": "Self-heal harness v1", "detail": "Score About/Team/Impressum links; hidden tabs; merge; max 4 pages / depth 2.", "files": ["lib/harness.js", "background/service-worker.js"], "done_when": "A sparse homepage fills executives from /leadership."},
    {"id": "P01", "phase": "Q", "status": "done", "title": "Engine unit tests", "detail": "Add tests/engine.test.mjs covering extractFromMarkdown, scoreLinks, mergeRecords (including dirty-field conflicts), maskValue, isComplete. Run with node --test.", "files": ["tests/engine.test.mjs", "package.json"], "done_when": "node --test tests/engine.test.mjs exits 0 with cases for merge conflict, JP/DE fixtures, and DLP mask.", "completed_at": "2026-09-10T17:30:00Z", "notes": "Added node --test suite covering extract, scoreLinks, dirty merge conflicts, JP/DE fixtures, and DLP mask."},
    {"id": "P02", "phase": "Q", "status": "done", "title": "Harness circuit-breaker tests", "detail": "Test max pages, visited-url registry, score threshold skip, and cache short-circuit without calling fetchPage.", "files": ["tests/harness.test.mjs", "lib/harness.js"], "done_when": "node --test tests/harness.test.mjs exits 0; a looping link graph cannot exceed MAX_PAGES.", "completed_at": "2026-09-10T18:31:00Z", "notes": "Added harness circuit-breaker tests; visited registry marks URLs before fetch so looping graphs stay within MAX_PAGES."},
    {"id": "P03", "phase": "4", "status": "done", "title": "Chunked DOM map-reduce", "detail": "If markdown would exceed 8k tokens, split by section (h1–h3), infer each chunk, merge records. Keep a single-pass path for small pages.", "files": ["lib/engine.js", "lib/nano.js", "content/content-script.js"], "done_when": "A 20k-word fixture is chunked; company_name and executives still merge into one record.", "completed_at": "2026-09-10T19:35:00Z", "notes": "Split oversized markdown on h1–h3, infer per chunk, merge into one record; serialize cap raised for long pages."},
    {"id": "P04", "phase": "3", "status": "done", "title": "Route inference through offscreen", "detail": "Service worker must not call LanguageModel. Send AETHER_OFFSCREEN_INFER to the offscreen document and use the result. Heuristic may still run in SW if offscreen is unavailable.", "files": ["background/service-worker.js", "offscreen/offscreen.js", "lib/harness.js"], "done_when": "Primary and second-pass infer go through chrome.runtime.sendMessage to offscreen when the document exists.", "completed_at": "2026-09-10T20:30:00Z", "notes": "Harness accepts infer(); SW sends AETHER_OFFSCREEN_INFER and falls back to extractFromMarkdown without LanguageModel."},
    {"id": "P05", "phase": "1", "status": "done", "title": "Options page", "detail": "chrome_url_overrides-free options.html: default PII mask, max pages (2–6), force-heuristic toggle, clear-cache button. Persist in chrome.storage.sync.", "files": ["options/index.html", "options/options.js", "manifest.json", "background/service-worker.js"], "done_when": "chrome://extensions options opens; harness reads max pages from storage.", "completed_at": "2026-09-10T21:33:14Z", "notes": "Added options_ui; sync settings drive harness maxPages and force-heuristic."},
    {"id": "P06", "phase": "1", "status": "done", "title": "Persist undo history", "detail": "Store last 20 record snapshots per origin in chrome.storage.session (fallback local). Restore undo/redo across side-panel reloads.", "files": ["sidepanel/panel.js", "lib/history.js"], "done_when": "Edit a field, close and reopen the panel, Undo still reverts.", "completed_at": "2026-09-10T22:30:00Z", "notes": "Persisted record plus 20 undo/redo snapshots per origin in session storage with local fallback."},
    {"id": "P07", "phase": "3", "status": "done", "title": "Background tab cap + cleanup", "detail": "Never more than 2 hidden heal tabs at once. Always remove the tab in a finally. Timeout 8s already exists — surface failures in the log.", "files": ["background/service-worker.js"], "done_when": "Three scored links never leave more than two extra tabs open; all are closed after the run.", "completed_at": "2026-09-10T23:30:00Z", "notes": "Heal fetches use a 2-slot semaphore, always remove the tab in finally, and log 8s timeouts to the panel."},
    {"id": "P08", "phase": "1", "status": "done", "title": "Stable source IDs in the DOM", "detail": "During serialize, stamp data-aether-id on heading/email/phone/person nodes. Highlight uses that id instead of XPath-on-snippet.", "files": ["content/content-script.js", "sidepanel/panel.js"], "done_when": "Verify source scrolls to the exact node on a live corporate page with duplicate names.", "completed_at": "2026-09-11T00:31:00Z", "notes": "Serialize stamps data-aether-id on heading/email/phone/person nodes; Verify source highlights by id, not XPath."},
    {"id": "P09", "phase": "1", "status": "done", "title": "CSV export and clipboard", "detail": "Export executives as CSV. Copy-record copies JSON (respecting PII mask) to clipboard with a short toast.", "files": ["sidepanel/panel.js", "sidepanel/index.html", "lib/engine.js"], "done_when": "CSV downloads with name,role,email columns; copy writes masked JSON when PII is on.", "completed_at": "2026-09-11T01:30:00Z", "notes": "CSV export of executives (name,role,email) and Copy writes PII-aware JSON with a toast."},
    {"id": "P10", "phase": "2", "status": "done", "title": "Side panel UI locale", "detail": "chrome.i18n with _locales/en, de, ja for chrome-facing strings (Extract, Record, Mask PII, C-suite). Page extraction already preserves Unicode.", "files": ["_locales/en/messages.json", "_locales/de/messages.json", "_locales/ja/messages.json", "sidepanel/panel.js", "manifest.json"], "done_when": "Chrome UI language de shows German chrome strings; extracted JP names are unchanged.", "completed_at": "2026-09-11T02:30:00Z", "notes": "Added chrome.i18n locales en/de/ja; side panel chrome strings use getMessage; extracted field values stay as-is."},
    {"id": "P11", "phase": "5", "status": "done", "title": "Mask PII before cache write", "detail": "Optional storage flag maskCache. When on, emails/phones are masked in chrome.storage.local schemas. Default off so re-extract can still heal. Document in README.", "files": ["background/service-worker.js", "lib/engine.js", "options/options.js", "README.md"], "done_when": "With the flag on, storage get of the schema has no raw email/phone.", "completed_at": "2026-09-11T03:34:00Z", "notes": "maskCache (default off) runs maskRecordForCache before writing domain schemas so stored email/phone are redacted."},
    {"id": "P12", "phase": "Q", "status": "done", "title": "Web Store listing draft + test script", "detail": "STORE.md (name, short/long description, privacy), and tests/manual.md with a 10-step unpacked QA checklist.", "files": ["STORE.md", "tests/manual.md", "README.md"], "done_when": "STORE.md and tests/manual.md exist and README links both.", "completed_at": "2026-09-11T04:33:00Z", "notes": "Added STORE.md listing copy plus tests/manual.md 10-step QA; README links both."},

    {"id": "R01", "phase": "H", "status": "done", "title": "Attach sourceIds to record fields", "detail": "Map content-script doc.sources (data-aether-id stamps) onto field and executive metadata during extractFromMarkdown and Nano applyJson. Panel Verify source must prefer sourceId over snippet. Keep snippet as fallback only.", "files": ["lib/engine.js", "lib/nano.js", "sidepanel/panel.js", "tests/engine.test.mjs"], "done_when": "After extract, company_name/email/phone/exec fields carry sourceId when a matching stamped source exists; unit test asserts sourceId is set from a fixture sources array.", "completed_at": "2026-09-11T07:06:21Z", "notes": "extractFromMarkdown/applyJson attach sourceId from doc.sources; unit test covers company/email/phone/exec."},
    {"id": "R02", "phase": "H", "status": "done", "title": "Validate Nano JSON against CompanyRecord schema", "detail": "Add a pure validateCompanyPayload (or equivalent) that accepts only known keys, string fields, and executives with name. Reject or strip invalid types. applyJson and infer path must run validation before merge; invalid payload falls back to heuristic for that pass.", "files": ["lib/engine.js", "lib/nano.js", "tests/engine.test.mjs"], "done_when": "Malformed Nano JSON (wrong types, extra exec without name) does not poison the record; tests cover valid accept and invalid reject/fallback.", "completed_at": "2026-09-11T07:30:00Z", "notes": "validateCompanyPayload strips unknown keys and nameless execs; infer falls back to heuristic on bad types."},
    {"id": "R03", "phase": "H", "status": "done", "title": "Broader heuristic executive patterns", "detail": "Extend extractExecs beyond '- **Name** — Role — email'. Support plain 'Name — Role', 'Name, Title', and DE/JP role tokens without requiring markdown bold. Add fixture cases for non-bold leadership lists.", "files": ["lib/engine.js", "tests/engine.test.mjs"], "done_when": "At least three additional executive line formats extract name+role in unit tests; existing bold-format tests still pass.", "completed_at": "2026-09-11T07:34:00Z", "notes": "extractExecs now parses Name — Role, Name, Title, and DE/JP role lines without bold; existing bold fixtures still pass."},
    {"id": "R04", "phase": "H", "status": "done", "title": "Score links from heal pages", "detail": "Harness depth loop must union scoreLinks from each successfully fetched heal doc (not only startDoc.links), still under visited set, SCORE_THRESHOLD, and maxPages. Same-origin preferred.", "files": ["lib/harness.js", "tests/harness.test.mjs"], "done_when": "A homepage with only a generic About link can discover /leadership via the About page's links within MAX_PAGES; test proves heal-page links are fetched.", "completed_at": "2026-09-11T08:32:00Z", "notes": "Heal docs union their links into the scored pool so About can discover /leadership within MAX_PAGES."},
    {"id": "R05", "phase": "H", "status": "done", "title": "On-demand content script injection", "detail": "Remove always-on content_scripts from manifest if feasible. Inject content/content-script.js via chrome.scripting.executeScript only when serializing the active or heal tab (SW already has a fallback path). Keep AETHER_HIGHLIGHT working after inject. Document permission rationale in STORE.md.", "files": ["manifest.json", "background/service-worker.js", "STORE.md"], "done_when": "manifest has no persistent all-URL content_scripts (or equivalent on-demand-only design); extract and highlight still work after Load unpacked.", "completed_at": "2026-09-11T09:30:00Z", "notes": "Removed always-on content_scripts; SW injects content-script.js on serialize and highlight."},
    {"id": "R06", "phase": "H", "status": "done", "title": "Slim cache payload + schema version", "detail": "Stop persisting full markdownByUrl by default in chrome.storage.local. Cache fields, executives, pagesVisited, language, extractedAt, and short sourceSnippet only. Add schemaVersion and optional TTL (e.g. 7d) so stale entries are ignored. maskCache still applies.", "files": ["background/service-worker.js", "lib/engine.js", "tests/engine.test.mjs", "README.md"], "done_when": "Cached blob has no multi-kilobyte markdown map; old-version or expired entries are treated as cache miss.", "completed_at": "2026-09-11T10:30:00Z", "notes": "Cache writes schemaVersion 2 slim blobs (no markdownByUrl); TTL 7d and old versions miss; maskCache still applies."},
    {"id": "R07", "phase": "H", "status": "pending", "title": "Correct offscreen document reasons", "detail": "Align chrome.offscreen.createDocument reasons/justification with actual work (Prompt API / inference isolation, not DOM_PARSER if unused). Ensure create still succeeds on Chrome 123+.", "files": ["background/service-worker.js"], "done_when": "Offscreen creation uses an accurate reason string allowed by the Offscreen API; inferViaOffscreen still returns records when the document exists."},
    {"id": "R08", "phase": "H", "status": "pending", "title": "Gold-set fixtures + quality metrics", "detail": "Add tests/fixtures/ with EN/DE/JP serialized docs (markdown + expected field subset). Add tests/quality.test.mjs that scores precision/recall for company_name, email, address, executive.name on the heuristic path. Document how to refresh the table in README.", "files": ["tests/fixtures/", "tests/quality.test.mjs", "README.md", "package.json"], "done_when": "node --test tests/quality.test.mjs exits 0 and prints or asserts minimum recall thresholds on the fixture pack."},
    {"id": "R09", "phase": "H", "status": "pending", "title": "GitHub Actions CI for unit tests", "detail": "Add .github/workflows/test.yml: on push/PR to main, run node --test tests/*.test.mjs (and quality if present). No secrets required.", "files": [".github/workflows/test.yml", "package.json", "README.md"], "done_when": "Workflow file exists; README mentions CI; locally npm test still passes."},
    {"id": "R10", "phase": "H", "status": "pending", "title": "Richer executive merge", "detail": "mergeRecords matches executives by normalized name. Fill empty role/email/linkedin from the patch; if both sides have different non-empty email and base is dirty/verified, push an exec conflict (or field-level conflict list extension). Prefer higher confidence when not dirty.", "files": ["lib/engine.js", "tests/engine.test.mjs"], "done_when": "Unit tests: same-name exec gains email from patch; dirty exec email is not overwritten and surfaces a conflict."},
    {"id": "R11", "phase": "H", "status": "pending", "title": "README results and guarantees section", "detail": "Replace outdated 'remaining work in PLAN' copy. Add Results & guarantees: operating bounds table (max pages, heal tabs, timeout, chunk budget, undo depth, locales), privacy checklist, engineering status (v2.1 done, v2.2 hardening), and placeholder or live quality metrics from R08.", "files": ["README.md"], "done_when": "README no longer says plan items remain for v2.1; contains a measurable bounds table and privacy checklist."},
    {"id": "R12", "phase": "H", "status": "pending", "title": "Force re-extract control in side panel", "detail": "Add a clear UI control (button or modifier) that sends AETHER_EXTRACT with force:true so domain cache is skipped. i18n strings for en/de/ja. Keep one-click Extract for the default cached path.", "files": ["sidepanel/index.html", "sidepanel/panel.js", "_locales/en/messages.json", "_locales/de/messages.json", "_locales/ja/messages.json"], "done_when": "User can force a fresh extract that bypasses chrome.storage.local schema cache from the side panel."}
  ]
}
